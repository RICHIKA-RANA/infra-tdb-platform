---
# Galaxy collections required by the TalkingDB Ansible tree.
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.docker
    version: ">=3.10.0,<4.0.0"
  - name: ansible.posix
    version: ">=1.5.0,<2.0.0"
